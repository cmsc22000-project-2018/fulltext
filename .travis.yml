language: c
compiler: gcc
before_script:
    - echo "Running Travis CI tests on /dev branch build"

script:
    # pwd: fulltext
    - make ftsh clean && make -C tests clean

after_script:
    - echo "Finished Travis CI tests on /dev branch build"

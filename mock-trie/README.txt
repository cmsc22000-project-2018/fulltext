Sprint 2 Mock Trie

Attempted to implement the following mock trie:

		""
	a	b	o
  an	at	be	of
and ant		bee

Start in mtrie.c main() with:

		""
	a	b	o

With insert:
if key == an, at, and, ant;
if key == be, bee;
if key == of
